# 🗂️ Log Archive CLI Tool

A simple Linux command-line tool that archives system logs by compressing them into `.tar.gz` files.  
This tool helps maintain system cleanliness while preserving logs for future reference.

---

## 🚀 Features

- Accepts log directory as an argument
- Compresses logs into timestamped `.tar.gz` archive
- Stores archives in a dedicated folder
- Maintains archive activity log
- Designed to run on any Unix/Linux server

---

## 🛠️ Technologies Used

- Bash Shell Scripting
- Linux CLI Utilities (`tar`, `date`, `mkdir`)
- Git & GitHub
- AWS EC2 (Testing Environment)

---

## 📦 Project Structure

